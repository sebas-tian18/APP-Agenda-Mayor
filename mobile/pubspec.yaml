name: mobile
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_calendar: ^27.1.53
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  english_words: ^4.0.0
  provider: ^6.0.0
  dio: ^5.7.0
  get: ^4.6.5
  iconsax: ^0.0.8
  flutter_secure_storage: ^7.0.0
  async: ^2.11.0
  characters: 1.3.0 # Alineado con flutter_test
  collection: ^1.18.0
  http_parser: ^4.0.2
  meta: ^1.15.0
  path: ^1.9.0
  platform: ^3.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
